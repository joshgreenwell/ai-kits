// origin: synthetic
// ref: JG-153 D-hook-changed
// completeness: complete (same event and matcher, different command)
// excerpt_or_raw: raw
{
  "permissions": {
    "allow": [
      "Bash(npm test)"
    ],
    "deny": [
      "Bash(curl *)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo pre-bash"
          }
        ]
      }
    ]
  }
}

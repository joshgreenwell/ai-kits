// origin: synthetic
// ref: JG-153 D-mode-widened / JG-154 I5
// completeness: complete (default → bypassPermissions)
// excerpt_or_raw: raw
{
  "permissions": {
    "allow": [
      "Bash(npm test)"
    ],
    "deny": [
      "Bash(curl *)"
    ],
    "defaultMode": "default"
  }
}

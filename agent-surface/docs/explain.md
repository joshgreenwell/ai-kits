<!-- Generated by scripts/render-explain.mjs from the explain registry (src/explain.ts). Do not edit by hand; a test compares this file with the generator's output. -->

# `agent-surface explain <ID>`

Every ID below is accepted by `agent-surface explain <ID>` and prints the same facts and text. Interpretations are the closed list dated **2026-09-07** (the `semantics_doc_date` of every snapshot and diff); anything outside it is `unresolved` and never rendered as clean.

## Interpretations (§3.6)

### `I1` — Rule-string shadowing (deny → ask → allow, first match)

- tier: projected
- doc section: Permission rules > How permission rules are evaluated
- semantics doc date: 2026-09-07
- summary: The same normalized rule string in deny shadows the string in ask and allow; in ask it shadows allow. A shadowed rule grants nothing and is reported as neutral.

Claude Code checks deny rules first, then ask rules, then allow rules, and stops at the first match. When the identical normalized rule string appears in an earlier list, the later rule can never be the first match, so adding or removing it changes nothing about what the agent may do while the earlier rule stands. agent-surface reports such a delta with direction neutral and flag shadowed. The claim is projected: it relies on string identity after normalization (`docs/normalization.md`) and on the documented list order; overlapping but different strings (a prefix rule that covers an exact rule) are not interpreted. A deny added together with an allow for the same string therefore counts as a narrowing plus a neutral, never as a proven expansion (JG-155).

### `I2` — Whole-tool vs scoped permission rules

- tier: proven
- doc section: Permission rules > Tool-specific permission rules
- semantics doc date: 2026-09-07
- summary: Bash / Bash(*) (and any bare tool name or Tool(*)) is a whole-tool rule; Bash(...) with a spec is scoped.

The documentation states that a rule consisting of a tool name alone matches any use of that tool, and that `Bash(*)` is equivalent to `Bash`. agent-surface assigns breadth whole_tool to both spellings (they remain distinct keys; see docs/normalization.md) and to every other bare tool name, including `mcp__server` and `mcp__server__tool`. `Bash(<spec>)` is scoped: it matches only what the spec describes, and I3/I4 decide whether that is an exact command, a prefix, or an interior-wildcard glob. The claim is proven because the documentation states it directly.

### `I3` — Trailing-wildcard Bash breadth: prefix, exact, or glob

- tier: projected
- doc section: Permission rules > Tool-specific permission rules > Bash
- semantics doc date: 2026-09-07
- summary: Bash(x *) and Bash(x:*) are prefix rules (projected); Bash(x) is exact (proven); any other wildcard placement is a glob with unresolved breadth.

The documentation describes `Bash(npm run test:*)` as matching commands that start with `npm run test`, and a rule without a wildcard as matching the exact command. agent-surface reads a trailing ` *` or `:*` as a prefix rule (both spellings share one key) and marks the breadth projected: prefix matching is documented, but its token boundaries and the equivalence of the two spellings are inferred. A rule with no wildcard is breadth exact and proven. A wildcard anywhere else (`Bash(git * main)`, `Bash(*.sh)`) has no documented matching rule; agent-surface records breadth glob, flags breadth_unresolved, and lists the delta under unresolved so a reviewer decides. The exit code treats a prefix or exact allow as a decided scoped-allow, and a glob allow as unresolved (exit 2 by default).

### `I4` — Wildcard before a subcommand is broad

- tier: projected
- doc section: Permission rules > Tool-specific permission rules > Bash
- semantics doc date: 2026-09-07
- summary: A `*` followed by more text in a Bash spec (Bash(git * main)) is flagged broad: it can match many unrelated commands.

The documentation only describes a trailing wildcard. When a `*` is followed by further text, the part after it does not constrain what the wildcard absorbed: `Bash(git * main)` covers `git push --force main` as well as `git checkout main`. agent-surface flags such rules broad and keeps breadth glob (from I3). The claim is projected because the exact matching behaviour is not documented; the flag exists so a reviewer looks at the rule rather than trusting its readable parts.

### `I5` — defaultMode: only bypassPermissions, auto and dontAsk widen

- tier: proven
- doc section: Permission modes
- semantics doc date: 2026-09-07
- summary: bypassPermissions, auto and dontAsk widen; acceptEdits, plan and default do not; dontAsk is not allow-everything; an unknown value is unresolved and never widens.

The permission-modes documentation lists default, acceptEdits, plan, dontAsk, auto and bypassPermissions. bypassPermissions skips every prompt; auto lets the model act without asking; dontAsk auto-denies anything that would have prompted, so the agent can still run everything already allowed without a human in the loop. agent-surface treats those three as widening. acceptEdits (auto-accepts file edits), plan (read-only) and default are not treated as widening, and a move between them is neutral. dontAsk is explicitly not allow-everything: it never grants what an allow rule does not; the note on the delta says so. Any other value is unknown to the documentation of the cited date: direction unknown, tier unresolved, never widening.

### `I6` — Hook presence per event and matcher

- tier: proven
- doc section: Hooks reference > Configuration
- semantics doc date: 2026-09-07
- summary: A hook under hooks.<event>[].hooks[] runs automatically for that event and matcher; the command is recorded and hashed, never executed.

Hooks are configured per event (PreToolUse, PostToolUse, Stop, …) with an optional matcher regular expression over tool names; an absent or empty matcher matches every tool. agent-surface proves presence only: which event, which matcher, and the sha256 of the command text (which is part of the entry key, so a changed command is a changed entry). It never executes the command, never models the hook's decision output, and does not model plugin-provided hooks (see the not-interpreted list).

### `I7` — MCP transport: literal http:// to a non-loopback host is plaintext

- tier: proven / unresolved
- doc section: MCP > Project-scope servers (.mcp.json) and remote HTTP/SSE servers
- semantics doc date: 2026-09-07
- summary: Literal http:// to a non-loopback host is proven plaintext (flag plaintext); loopback and https:// are proven and unflagged; ${VAR} in a URL or command is unresolved.

.mcp.json declares stdio servers (command + args) and remote servers (type http or sse with a url). Claude Code expands ${VAR} references in .mcp.json at run time; agent-surface never does, so any URL or command containing a variable reference has an unknown target and is reported unresolved with flag variable_reference. A literal http:// URL whose host is not loopback (localhost, 127.0.0.0/8, [::1]) sends the MCP session in plaintext: proven, flagged plaintext. Loopback http:// is proven and not flagged; https:// is proven and not flagged. A stdio command is recorded and never executed. A transport other than stdio, http or sse, or a scheme other than http(s), is outside the documented set and unresolved.

### `I8` — Rule shapes ignored by Claude Code

- tier: proven
- doc section: Permission rules > Tool-specific permission rules (Read & Edit, MCP) and notes on unsupported rule forms
- semantics doc date: 2026-09-07
- summary: Write(path), NotebookEdit(path), Glob(path), MultiEdit(path), mcp__…(…) with parentheses, and unanchored allow globs like Read(*.env) are ignored: neutral, flagged ignored_by_claude_code.

The documentation defines path-scoped rules for Read and Edit only, and MCP rules as mcp__server or mcp__server__tool without parentheses. A path in parentheses on Write, NotebookEdit, Glob or MultiEdit, or parentheses on an mcp__ name, therefore matches nothing; an allow glob with no anchoring segment (Read(*.env)) is likewise documented as not matching. agent-surface reports such entries with direction neutral (they grant nothing and deny nothing) and tier proven, flagged ignored_by_claude_code, so a reviewer sees that the rule is inert instead of trusting it. A deny of an ignored shape blocks nothing.

## Explicitly not interpreted

### `N-compound-command` — Compound-command splitting (`&&`, `;`, `|`)

- tier: unresolved
- detected: yes
- semantics doc date: 2026-09-07

A Bash rule whose spec contains &&, ||, ; or | is not split into its parts. Whether Claude Code matches the rule against the whole line or each command is not modeled; the delta is unresolved.

### `N-wrapper-stripping` — Wrapper stripping (`sudo`, `env`, `time`, `xargs`)

- tier: unresolved
- detected: yes
- semantics doc date: 2026-09-07

A Bash rule that starts with a wrapper such as sudo, env, time or xargs is not reduced to the wrapped command. What the wrapper eventually runs is unresolved.

### `N-env-assignment` — Env-assignment stripping (`FOO=bar cmd`)

- tier: unresolved
- detected: yes
- semantics doc date: 2026-09-07

A Bash rule that starts with a NAME=value assignment is not reduced to the command after it. The assignment's effect on the command is unresolved.

### `N-redirect` — Redirect checks (`>`, `<`)

- tier: unresolved
- detected: yes
- semantics doc date: 2026-09-07

A Bash rule containing a shell redirect is not analysed for what it writes or reads. The delta is unresolved.

### `N-path-anchoring` — Path anchoring (`/`, `//`, `~`)

- tier: unresolved
- detected: yes
- semantics doc date: 2026-09-07

Read/Edit path rules resolve differently depending on their anchor (// absolute, ~ home, / relative to the settings file, otherwise relative to the working directory). agent-surface records the path as written and does not resolve it; a path rule delta is unresolved.

### `N-depth-semantics` — Depth semantics (`*` vs `**`)

- tier: unresolved
- detected: yes
- semantics doc date: 2026-09-07

How far a path pattern descends (one segment for *, any depth for **) is not modeled. A path rule delta is unresolved.

### `N-symlink-pairing` — Symlink pairing

- tier: unresolved
- detected: yes
- semantics doc date: 2026-09-07

Whether a path rule also covers the link or target of a symlink is not modeled. A path rule delta is unresolved.

### `N-plugin-provided` — Plugin-provided hooks and servers (`enabledPlugins`)

- tier: unresolved
- detected: yes
- semantics doc date: 2026-09-07

Hooks and MCP servers contributed by plugins are not read. A change to enabledPlugins is unresolved and the assumptions header flags non-empty enabledPlugins.

### `N-skill-allowed-tools` — Skill `allowed-tools`

- tier: unresolved
- detected: no (files not read in V0)
- semantics doc date: 2026-09-07

Skill frontmatter (.claude/skills/*/SKILL.md) is not among the V0 inputs; a change there produces no entry and no claim. Listed so the absence is explicit.

### `N-subagent-frontmatter` — Subagent frontmatter

- tier: unresolved
- detected: no (files not read in V0)
- semantics doc date: 2026-09-07

Subagent definitions (.claude/agents/*.md, including their tools list) are not among the V0 inputs; a change there produces no entry and no claim. Listed so the absence is explicit.

## Direction rules (JG-153)

### `D-allow-added` — allow rule added

- direction: widens
- tier: proven
- cites: I2 (Permission rules > Tool-specific permission rules); I3 (Permission rules > Tool-specific permission rules > Bash); I4 (Permission rules > Tool-specific permission rules > Bash); I1 (Permission rules > How permission rules are evaluated)

A permissions.allow rule was added. Once the workspace is trusted, a matching allow rule lets the tool run without asking. Breadth (whole tool, exact, prefix, glob) comes from I2/I3/I4 and decides between the whole-tool-allow and scoped-allow categories; I1 may downgrade the delta to neutral when the same string is denied or asked.

### `D-ask-added` — ask rule added

- direction: neutral
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

A permissions.ask rule was added. It makes Claude Code prompt for the match; it grants nothing by itself, so the delta is annotated as neutral.

### `D-deny-added` — deny rule added

- direction: narrows
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

A permissions.deny rule was added. Deny is checked first, so the match can no longer run: the control surface narrows.

### `D-allow-removed` — allow rule removed

- direction: narrows
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

A permissions.allow rule was removed; whatever it allowed without asking now falls back to the default mode.

### `D-ask-removed` — ask rule removed

- direction: neutral
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

A permissions.ask rule was removed. Symmetric to D-ask-added: the prompt is gone but nothing is granted by the removal itself; annotated as neutral.

### `D-deny-removed` — deny rule removed

- direction: widens
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

A permissions.deny rule was removed; whatever it blocked is no longer blocked by the repository (category deny-removed). Proven from the list semantics regardless of the rule's breadth.

### `D-hook-added` — hook added

- direction: widens
- tier: proven
- cites: I6 (Hooks reference > Configuration)

A hook entry (event, matcher, sha256 of the command) was added; Claude Code will run the command automatically (category hook, I6).

### `D-hook-removed` — hook removed

- direction: narrows
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

A hook entry was removed; the command no longer runs automatically.

### `D-hook-changed` — hook command changed

- direction: widens
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

The command behind an existing event and matcher changed (one removed key and one added key with the same hook:<event>:<matcher>: prefix in the same file are paired). A different command runs automatically: treated as a new hook (category hook).

### `D-hook-attrs-changed` — hook attributes changed

- direction: neutral
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

The hook's command is unchanged (same key) but type, timeout or prompt text differs. Annotated as neutral.

### `D-mcp-added` — MCP server added

- direction: widens
- tier: proven
- cites: I7 (MCP > Project-scope servers (.mcp.json) and remote HTTP/SSE servers)

A server was added to .mcp.json; once approved it exposes new tools to the agent (category mcp, I7).

### `D-mcp-removed` — MCP server removed

- direction: narrows
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

A server was removed from .mcp.json.

### `D-mcp-changed` — MCP transport, command, args or url changed

- direction: widens
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

The server keeps its name but what runs or where it connects changed; treated like a new server (category mcp).

### `D-mcp-attrs-changed` — MCP env/header key names or extra fields changed

- direction: unknown
- tier: unresolved
- cites: the JG-153 direction table (no interpretation involved)

Only env or header key names, or fields outside the documented set, changed. The effect is not modeled; unresolved.

### `D-enable-all-mcp` — enableAllProjectMcpServers set to true

- direction: widens
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

enableAllProjectMcpServers became true: every server in .mcp.json is auto-approved (category mcp).

### `D-enable-all-mcp-off` — enableAllProjectMcpServers no longer true

- direction: narrows
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

enableAllProjectMcpServers went from true to false or was removed while true; servers need approval again.

### `D-mode-widened` — defaultMode moved to bypassPermissions, auto or dontAsk

- direction: widens
- tier: proven
- cites: I5 (Permission modes)

permissions.defaultMode was set to a widening mode from an absent or non-widening one (category mode, I5).

### `D-mode-narrowed` — defaultMode left a widening mode

- direction: narrows
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

permissions.defaultMode moved from bypassPermissions, auto or dontAsk to plan, default or acceptEdits, or was removed while widening (the effective mode then comes from settings V0 does not read).

### `D-mode-neutral` — defaultMode set, removed or changed within one class

- direction: neutral
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

defaultMode changed between two widening modes, between two non-widening modes, or was set to / removed from a non-widening value. Annotated as neutral.

### `D-mode-unknown` — defaultMode value not documented

- direction: unknown
- tier: unresolved
- cites: I5 (Permission modes)

A defaultMode value outside the documented set is involved; never treated as widening (I5).

### `D-hooks-disabled` — disableAllHooks set to true

- direction: narrows
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

disableAllHooks went false → true (or was added as true); no configured hook runs.

### `D-hooks-reenabled` — disableAllHooks no longer true

- direction: widens
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

disableAllHooks went true → false or was removed while true; every configured hook runs again (category hooks-reenabled).

### `D-dir-added` — additionalDirectories entry added

- direction: widens
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

permissions.additionalDirectories gained a path that Claude Code treats like the working directory (category directory). The path is recorded as written; anchoring is not interpreted.

### `D-dir-removed` — additionalDirectories entry removed

- direction: narrows
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

permissions.additionalDirectories lost a path.

### `D-flag-default` — boolean flag set to or removed at its default

- direction: neutral
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

enableAllProjectMcpServers or disableAllHooks was written as false or removed while false; the effective value did not change.

### `D-moved` — same key and value, different file

- direction: neutral
- tier: proven
- cites: the JG-153 direction table (no interpretation involved)

The key exists on both sides with an identical value but its location changed, e.g. from .claude/settings.json to the tracked .claude/settings.local.json. Reported once as changed (moved), never as removed plus added.

### `D-unknown` — shape not in the direction table

- direction: unknown
- tier: unresolved
- cites: the JG-153 direction table (no interpretation involved)

sandbox, env, helper commands, plugin lists, disableBypassPermissionsMode, unknown keys and credential-like literals have no direction rule. The delta is reported as unresolved so it is never rendered as clean.

## Verdict categories (JG-155)

### `hook` — New hook or changed hook command

- fails by default: yes

A hook entry was added, or the command behind an existing event/matcher changed (the entry key carries sha256(command), so a changed command is a new key paired with the removed one). Claude Code runs hook commands automatically; presence is proven, the command is recorded and never executed.

### `mcp` — New MCP server, changed transport, or enableAllProjectMcpServers

- fails by default: yes

An MCP server was added to .mcp.json, its command/args/url/transport changed, or enableAllProjectMcpServers became true (which auto-approves every server in .mcp.json). A server with a variable-expanded URL is still in this category but unresolved.

### `mode` — defaultMode changed to bypassPermissions, auto or dontAsk

- fails by default: yes

permissions.defaultMode moved to a widening mode from an absent or non-widening one. acceptEdits, plan and default never land here (interpretation I5).

### `whole-tool-allow` — Whole-tool allow rule added

- fails by default: yes

An allow rule with breadth whole_tool (a bare tool name such as Bash or Read, or Tool(*)) was added. It grants every use of that tool without asking once the workspace is trusted (interpretation I2).

### `directory` — additionalDirectories entry added

- fails by default: yes

permissions.additionalDirectories gained a path. Claude Code treats it like the working directory. The path is recorded as written; anchoring is not interpreted.

### `hooks-reenabled` — disableAllHooks turned off

- fails by default: yes

disableAllHooks went from true to false (or was removed while true), so every configured hook runs again.

### `deny-removed` — Deny rule removed

- fails by default: yes

A permissions.deny rule disappeared. Whatever it blocked is no longer blocked by the repository; the direction is proven from the list semantics regardless of the rule's breadth.

### `scoped-allow` — Scoped allow rule added (annotate-only by default)

- fails by default: no (annotate-only)

An allow rule with breadth exact, prefix or glob (Bash(npm test), Bash(npm run *), Bash(git * main)) or an uninterpreted path rule was added. Annotated but not failing unless --fail-on names scoped-allow; a glob breadth is additionally listed as unresolved (interpretations I3/I4).

### `projected` — Pseudo-category: projected widenings fail

- fails by default: no

Naming projected in --fail-on makes every widening delta whose direction or breadth claim is projected exit 1. Naming only projected keeps the default failing categories; naming it with other categories restricts the failing set to those categories plus projected widenings.

## Flags

### `shadowed` — flag shadowed


the rule string also appears in an earlier-matching list (deny before ask before allow), so this rule grants nothing while the other stands (I1)

### `broad` — flag broad


a wildcard appears before a subcommand, e.g. Bash(git * main); the rule may match far more than its literal parts suggest (I4)

### `breadth_unresolved` — flag breadth_unresolved


the wildcard placement is not documented as prefix or exact matching; the delta is listed under unresolved (I3)

### `plaintext` — flag plaintext


an MCP URL uses literal http:// to a non-loopback host (I7)

### `variable_reference` — flag variable_reference


an MCP URL or command contains ${VAR} / $VAR; agent-surface never expands variables, so the target is unresolved (I7)

### `ignored_by_claude_code` — flag ignored_by_claude_code


the rule has a shape the Claude Code documentation says is ignored; it grants and denies nothing (I8)

### `tracked_local` — flag tracked_local


the entry lives in a tracked .claude/settings.local.json (local file shared via Git, trust-held by Claude Code)


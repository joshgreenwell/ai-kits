# USG-009: Fix account attribution and freshness for existing allowance collection

[Backlog index](README.md) · [Direction](../usage-direction.md)

Status: Done
Priority: P0
Scope: Core
Stage: 2. Collection
Dependencies: [USG-001](usg-001-metric-and-source-contract.md)
Created: 2026-09-13

## Outcome

Give each account a correctly attributed, timestamped set of current allowance windows.

## Current gap

Existing statusline/embedded/browser evidence has freshness gaps, and machine-wide hooks can assign a reading to the first binding rather than the verified account.

## Acceptance criteria

1. Recheck existing statusline, embedded rollout, and current browser reader paths; distinguish hook installation, successful execution, collected readings, and accepted uploads.
2. Bind each reading to verified provider identity/account where available. Quarantine ambiguous or changed identities rather than assigning them to the first account.
3. Preserve provider meter keys, labels, durations, units, reset anchors, and scope, including Spark and model-specific windows; select the newest relevant supported reading.
4. Show stale/missing readings honestly and preserve gap/reset/decrease handling in forecasts. A coverage-only receipt does not refresh the account meter.
5. Document attainable refresh behavior for each reader, prevent unrelated readers/settings from claiming coverage, and retain existing account data while identity is reconfirmed.

## Verification

Use two-account switching, ambiguous identity, stale/no-data runs, reset transitions, overlapping windows, Spark, and repeated-reading cases; obtain fresh readings on accessible active bindings.

Apply the common completion requirements in the [backlog index](README.md). Completion evidence is recorded below.

## Starting points

- [companion/crates/observatory/src/commands/statusline.rs](<../../companion/crates/observatory/src/commands/statusline.rs>)
- [companion/crates/observatory-adapters/src/readings.rs](<../../companion/crates/observatory-adapters/src/readings.rs>)
- [companion/crates/observatory-core/src/inbox.rs](<../../companion/crates/observatory-core/src/inbox.rs>)
- [lib/telemetry-contract.ts](<../../lib/telemetry-contract.ts>)
- [lib/telemetry-store.ts](<../../lib/telemetry-store.ts>)
- [docs/usage-system.md](<../../docs/usage-system.md>)

## Execution record

Completed on `kit-board/usg-009-allowance-identity-freshness` on September 14, 2026.

- Decisions: the Claude statusline reader owns the Claude allowance meter and moved from `claude_execution` into the `claude_account` adapter (parser version `2.0.0+statusline1`, adapter `claude_account`, channel `hook_snapshot`, reader `statusline`; record ids unchanged, so readings the Observatory already holds keep their ids). `allowance.claude_reader` gates that adapter with `off` as the only value that stops it, and mode `oauth_usage` — still unimplemented — keeps the statusline reader running as the documented fallback while the adapter reports `partial` / `not_implemented`; the local deny entry `allowance.claude_reader.statusline`, or a dotted prefix, removes the reader in either mode and keeps queued statusline readings on the machine. Codex embedded readings stay in `codex_execution`, because they are a by-product of the same rollout scan. This is a contract change: `CapabilityDimension` gains `allowance` and the per-adapter maximum rises from seven capability rows to eight, so the deployment order is server and the `20260914010000` migration first — envelope v2 has no runtime negotiation and `ConfigDocument` rejects unknown fields, so a companion carrying the new row must not be installed before the server that accepts it, and the migration must precede the server code that writes `basis`. An allowance reading that cannot be attributed to a verified account is held on the machine rather than published against a default binding.
- Companion: `observatory statusline` stamps each sample with the account signed in when it observed the reading, resolving the Claude config file as `OBSERVATORY_CLAUDE_CONFIG_FILE`, then `$CLAUDE_CONFIG_DIR/.claude.json`, then `~/.claude.json`, taking the account uuid out of `oauthAccount` and keeping only its hash, and caching that evidence per path by `stat` in `claude-identity-cache.json`. The hook writes one part file per changed reading (`<inbox>/<YYYY-MM-DDTHH>-<observed microseconds>.json`) on a changed `(used_percent, resets_at)` or a fifteen-minute heartbeat instead of overwriting an hour file, with the kept state `claude-statusline-latest.json` (keyed by stamp, then window) and the sidecar (gaining `last_offered_at` and `offered_windows_ever`; `published_windows` now means written this invocation) beside the inbox rather than inside it; the run, not the reader, prunes the inbox afterwards at `max(local_raw_retention_days, 2)` days by hour prefix. `claude_account` binds a stamped sample to the enabled Claude binding whose confirmed hash equals the stamp and that is free of identity conflict, and holds the rest in the schema-8 `allowance_quarantine` table (`identity_ambiguous`, `identity_unconfirmed` with the lone candidate binding recorded, `unpaired_identity`), releasing stamped rows by hash, unstamped held rows only to their recorded candidate once it confirms, never an unstamped ambiguous row, and pruning at `max(local_raw_retention_days, 7)` days unless a stamp still pairs with an enabled binding. A digest already stored or held is skipped before any binding decision through the new `allowance_slots(slot)` index. `prepare()` separates a switched account (`Changed`, not a conflict, so its own stamped readings still bind) from a real conflict, refreshes the roots pin when both sides name the same account, and declines to post an identity a sibling binding already holds or that two enabled unconfirmed siblings could both claim; `effective()` lets `ClaudeAccount` run while one enabled Claude binding is free of conflict. `claude_account` and `codex_execution` report the `allowance` capability row and `claude_execution` no longer reports one, because it no longer reads a meter; `doctor` gains a `claude_statusline` block and an `--offline` flag, and `setup` installs the hook into `$CLAUDE_CONFIG_DIR/settings.json` when that variable is set.
- Server: migration `20260914010000_allowance_basis_and_run_counts.sql` keeps `basis` on `allowance_readings` (default `reported`, checked against the wire values), restates `allowance_percent_view` with `basis` and its `security_invoker` option and grants, adds `companion_runs.accepted_by_type`, and indexes `allowance_readings` and `activity_requests` by `(binding_id, observed_at DESC)`. `lib/allowance-freshness.ts` states the single rule — stale when the age exceeds `max(120, 2 × cadence_minutes + 15)` minutes or the window has already reset — and `loadDashboard`, `quotaPace`, `quotaWindowState`, the Connections page, and the browser card all use it; `lib/allowance-meters.ts` gives each meter key a canonical card title. `loadDashboard` selects the current reading as the newest `observed_at` among the recognized readers `statusline`, `embedded`, and `web_backend`, ties by that order, and never lets an unrecognized reader win; `ingestUsage` stores `basis` and counts accepted, duplicate, and rejected records per record type, merged key-wise across the envelopes of one run; `confirmIdentity` answers 409 `identity_taken` for a hash a sibling binding of the install already holds; `listInstalls` exposes per binding `last_observation` and `last_received`, `duplicate_identity`, and per install `cadence_minutes`, `last_run_at`, and `accepted_by_type`; `/api/usage-connections` reports each browser source's last contact, newest observation, and receipt separately. Collector contact keeps its meaning and is reported beside the reading verdict, never folded into it.
- Synthetic coverage (`companion/crates/observatory-adapters/tests/allowance_identity.rs`, `companion/crates/observatory/tests/statusline.rs`, the adapter snapshots, and the web unit and integration suites): stamped samples for two bindings with distinct hashes where one is `Changed` by a local mismatch; a binding in conflict holding its stamped samples as `unpaired_identity` while the other binding's bind; an unstamped sample binding only to a lone confirmed binding, held as `identity_unconfirmed` for a lone unconfirmed one and as `identity_ambiguous` for two; two unconfirmed bindings holding everything until one is disabled and the other confirms; release only to the binding a hold was recorded for; an ambiguous unstamped hold that is never released, only pruned; release after a binding gains the hash, with a replay afterwards emitting nothing; a replayed digest skipped before any binding decision, including one already held; held rows absent from the dirty slots; pruning that keeps rows still pairing with a binding in conflict; the capability ladder with no samples (`hook_not_installed`, `hook_not_executing`, `hook_config_dir_mismatch`, `no_samples_offered`, `no_recent_samples`) and with them; the freshness threshold at plus or minus one minute for cadence 60 and at the two-hour floor for cadence 15; hook command parsing by the `statusline` token and `--config-dir` only, never the executable path; the `oauth_usage` fallback and the statusline deny; part files written on change, on a reset move, and on the heartbeat only; hour-prefix pruning that spares other file names, including with the reader denied; the identity cache served from a `stat`, missing a different path, caching a signed-out null, and holding no secret; a stamped sample round trip beside legacy files without the stamp; state schema 8 on a version-7 file; `claude_execution` reporting no allowance row and no statusline readings while `codex_execution` reports the eighth row; the statusline binary end to end with `CLAUDE_CONFIG_DIR` and `OBSERVATORY_CLAUDE_CONFIG_FILE` profiles, a hook naming another configuration directory, another account's hash quarantining with zero records, an offline dry run binding three samples, and `doctor` printing the hook and the quarantine without a sample path; and, on the server, a `basis` round trip through the ledger, the view, and `/api/usage-v2`, the migration proof on a row seeded before it (including `security_invoker`, the `basis` check, the seeded `accepted_by_type` default, and both indexes), newest-then-rank selection with an exact tie and a newer unknown reader, freshness at an install cadence override, a coverage-only envelope that advances contact and leaves `last_observation` and the ledgers untouched, `accepted_by_type` merged across two bodies of one run, the sibling-hash 409, the `listInstalls` shape, a browser zero-quota post, `readingFreshness` at plus or minus one minute for several cadences with the expired rule, the two-sample `quotaPace` and card agreement, `meterLabel` across both readers' labels, and the routing quota state with a quiet collector reported separately.
- Verification on September 14, 2026 (Windows host, Docker Desktop engine 28.3.0): in `kit-board/companion`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` (212 passed across 13 suites, 0 failed, including the new `allowance_identity` and statusline-binary suites and the new `claude_account` snapshot), `python scripts/fixtures.py check` (the parity `expected.json` and `MANIFEST.json` unchanged), and `cargo deny check` (advisories, bans, licenses, sources ok) passed; in `kit-board`, `npm test` (93 tests, 89 passed, 0 failed, 4 database-gated skips), `npm run typecheck`, `npm run build`, `npm run test:db` (11 migrations applied on `postgres:17-alpine`, database integration passed), the usage-schema byte compare against the vendored copy, and `git diff --check` passed. A local functional check ran the built binary against synthetic statusline JSON in scratch directories: the hook stamped both samples, wrote one part file, wrote none on an identical second call and one window on a changed reading, and `run --dry-run --offline` against a seeded cached config document bound three samples, reported the allowance capability, quarantined another account's samples as `unpaired_identity` with zero records, and pruned old part files with the reader denied. The production companion directory was never touched. A five-lens review with adversarial verification then confirmed seven findings (misattribution when two bindings both have a null hash, per-profile kept state and identity cache, the replay index, the release rule for unstamped holds, the "last run never" label, the selection test, and the `CLAUDE_CONFIG_DIR` case in the binary test); each was fixed with a regression test. A final recovery audit fixed two more identity boundaries: stamped samples now include their local account hash in the replay key so equal readings from two accounts remain distinct, and binding creation/confirmation serialize per install and refuse a sibling's hash. The same audit updated the locked `rustls` dependency from 0.23.44 to 0.23.45 after RUSTSEC-2026-0285 was published, then reran every suite above green.
- Remaining notes: fresh production readings on the active bindings are gated on the USG-025 deployment — the server and the `20260914010000` migration must be deployed before a companion carrying the `allowance` coverage row is installed — so only local synthetic evidence exists for this task. The parity fixture's sidecar is the older shape without `offered_windows_ever`, which the reader reads as unknown rather than "never offered"; regenerating the corpus is not required and `fixtures.py check` passes unchanged. `lib/http.ts` serializes only a message, so the sibling refusal carries `identity_taken` in its 409 message rather than as a machine-readable code, and the companion branches on the status alone. `lib/routing-store.ts` does not yet supply a binding's cadence to `QuotaRow`, so quota-state judges every window at the 60-minute default. Presentation of collection health beyond the Connections lines remains USG-014, and statusline-compatible browser meter keys remain USG-010.
